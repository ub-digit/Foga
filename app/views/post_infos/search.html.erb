<br>
<!-- Searchy searchy -->

<%= search_form_for @search, url: post_infos_search_path do |f| %>
	<!-- <div class="field">
		< %= f.label  "Titel börjar med" %>
		< %= f.check_box :title_start %>
		< %= f.text_field :title_start %>			
	</div> -->

	<div class="field">
		<%=  # to search for any of the terms existing in my_field
			 f.label :title_has_every_term, "Titel innehåller" %>
  		<%= f.text_field  :title_has_every_term %>
	</div>

	<div class="field">
		<%= f.label :publisher_cont, "Utgivare" %>
		<%= f.text_field :publisher_cont %>
	</div>
	<div class="field">
		<!-- ISSN should maybe be equal to? -->
		<%= f.label :issn_eq, "ISSN" %>
		<%= f.text_field :issn_eq %>
	</div>
	<div class="field">
		<%= f.label :operation_id_cont, "Åtgärd" %>
		<%= collection_select(:post_info, :operation_id, Operation.all.order("operation_type"), 
													:id, :operation_type, :prompt => "Välj") %>
		<!-- %= f.text_field :operation_id_cont %> -->
	</div>
	<div class="actions">
		<%= f.submit "Sök" %>
	</div>

<% end %>

<% if params[:commit] != nil %>
	<%= render 'searchpretty' %>
	<% elsif%>
	<%= "Nopes" %>
<% end %>

