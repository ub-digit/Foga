<!--  Toggle text saying how many days we are showing now 
      and link to the other option for days.
      I.e. only one link. Text with info should NOT be hardcoded. 
      If entering no. of days via url...
 -->

</head>
<body>
    <div class="container-fluid"> 
      <div class="row">         
         <div class="col-md-3"></div>
         <div class="col-md-5">
            <br>
            <table class="table table-bordered">
               <tr>
                  <th>
                  <div align="center">
                     <h4> <b> VISAR <%= @days %> DAGAR BAKÅT </b></h4>
                  </div>
                  </th>
                  
                  <th>
                  <div align="center">
                     <h4>
                     <% if @days > 7 %>
                        <%= link_to("SENASTE 7 DAGARNA", 
                        {:action => 'latest', :days => 7}) %>
                     <% else %>
                         <%= link_to("SENASTE 30 DAGARNA", 
                        {:action => 'latest', :days => 30}) %> 
                     <% end %>
                     </h4>
                  </div>
                  </th>
               </tr>
            </table>

         </div>
         <div class="col-md-2"></div>
      </div>
   </div>

   <center> 
      <%= will_paginate @post_infos, renderer: BootstrapPagination::Rails %>
   </center>
   <h3> <%= @post_infos.count %> Träffar </h3>

</body>
</html>


<%= render 'pretty' %>

