

<br>


<div class="container-fluid">
	<div class="row">
			<div class="col-md-2"></div>
			<div class="col-md-3">
	<h4>
	<b> Titel:  </b> <%= @post_info.title %> 	<br><br>
	<b> Åtgärd: 			</b><%  if Operation.find_by(id: @post_info.operation_id)   %>
					<%=	Operation.find_by(id: @post_info.operation_id).operation_type %> 
					<%  else %>
					<%=	"Okänd" %>
					<% end %>
 <br>
	<b>	Utgivare:  		</b><%= @post_info.publisher %> <br>
	<b>	ISSN:  				</b><%= @post_info.issn %> <br><br>
	<b>	Skapad av: 		</b><%= @post_info.created_by %> <br>
	<b>	Skapad:   		</b><%= @post_info.created_at.to_date %>  
</h4>
	</div>

	<div class="col-md-3">
	<h4>
	<b>	Kommentar:  	</b><%= @post_info.comment %><br><br>
	<b>	Updaterad av: </b><%= @post_info.updated_by %> <br>
	<b>	Updaterad: 		</b><%= @post_info.updated_at.to_date %> <br>

	</div>
	
	<div class="col-md-2"></div>
			
	</h4>	
</div>
	
	<br>
	<%= link_to "Redigera", edit_post_info_path %>
	<%= link_to "Radera", post_info_path(@post_info), 
			method: :delete, data: { confirm: "Är du säker?"} %>
	<br>
